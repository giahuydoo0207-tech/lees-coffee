{
  "name": "pocs-platform",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild app.jsx --bundle --minify --outfile=bundle.js --platform=browser --define:process.env.NODE_ENV=\\\"production\\\" && esbuild pos.jsx --bundle --minify --outfile=pos_bundle.js --platform=browser --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "dependencies": {
    "esbuild": "^0.21.5",
    "lucide-react": "^1.14.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "recharts": "^3.8.1"
  }
}
